{% extends 'base.html.twig' %}

{% block title %}{{ program.title }}/{{season.number}}/{{episode.number}}
{% endblock %}
{% block body %}

	<a href="{{ path('program_season_show', {program: program.id, season: season.id}) }}">
		Retour à la fiche de {{program.title}} saison {{ season.number }}
	</a>
	<div class="media">
		<img class="align-self-start mr-3" src="{{program.poster}}" alt="{{ program.title }}">
		<div class="media-body">
			<h1 class="mt-0">{{ program.title }}</h1>
            <h2> Saison {{ season.number }}</h2>
			<p>Episode {{ episode.number }} : {{ episode.title }}</p>
			<p>{{ episode.synopsis }}</p>
		</div>
	</div>
	

{% endblock %}